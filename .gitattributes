# Normalize line endings to LF in the repository (checked out native on Windows)
* text=auto eol=lf

# Treat the built standalone app as text but don't clutter diffs with it
Sids-EtherCAT-Configurator.html linguist-generated=true
package-lock.json linguist-generated=true
